---
title: U6 - Desarrollo Backend Básico
description: PHP y Node.js básico, conexión a base de datos, operaciones CRUD
---

## Objetivos de la unidad

- Comprender el funcionamiento del backend en una aplicación web
- Implementar un servidor básico con PHP y Node.js
- Conectar y operar sobre una base de datos MySQL
- Realizar operaciones CRUD completas

## Introducción

El backend es la capa del servidor que procesa peticiones, interactúa con la base de datos y devuelve respuestas al frontend. Esta unidad cubre PHP y Node.js como tecnologías backend de entrada.

## PHP básico

PHP es un lenguaje del lado del servidor ampliamente usado. Se ejecuta en el servidor y genera HTML dinámico.

### Sintaxis básica

```php
<?php
// Variables
$nombre = "Ana";
$edad = 22;
$precio = 29.99;

// Arrays asociativos
$usuario = [
    "nombre" => "Ana",
    "email" => "ana@example.com"
];

// Condicionales
if ($edad >= 18) {
    echo "Mayor de edad";
} else {
    echo "Menor de edad";
}

// Bucles
foreach ($usuario as $clave => $valor) {
    echo "$clave: $valor<br>";
}

// Funciones
function saludar($nombre) {
    return "Hola, $nombre!";
}

echo saludar("Ana");
?>
```

### Recibir datos del formulario

```php
<?php
// mostrar_formulario.php
if ($_SERVER["REQUEST_METHOD"] === "POST") {
    $nombre = htmlspecialchars($_POST["nombre"]);
    $email = htmlspecialchars($_POST["email"]);

    if (empty($nombre) || empty($email)) {
        echo "Todos los campos son obligatorios";
    } else {
        echo "Registrado: $nombre ($email)";
    }
}
?>

<form method="POST">
    <input type="text" name="nombre" placeholder="Nombre" required>
    <input type="email" name="email" placeholder="Email" required>
    <button type="submit">Enviar</button>
</form>
```

### Conexión a MySQL con PDO

```php
<?php
$host = 'localhost';
$db   = 'mi_proyecto';
$user = 'root';
$pass = '';
$charset = 'utf8mb4';

$dsn = "mysql:host=$host;dbname=$db;charset=$charset";
$options = [
    PDO::ATTR_ERRMODE            => PDO::ERRMODE_EXCEPTION,
    PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
    PDO::ATTR_EMULATE_PREPARES   => false,
];

try {
    $pdo = new PDO($dsn, $user, $pass, $options);
} catch (PDOException $e) {
    die("Error de conexión: " . $e->getMessage());
}
?>
```

### CRUD completo en PHP

```php
<?php
require_once 'conexion.php';

// CREATE
function crearUsuario($nombre, $email) {
    global $pdo;
    $sql = "INSERT INTO usuarios (nombre, email) VALUES (:nombre, :email)";
    $stmt = $pdo->prepare($sql);
    $stmt->execute(['nombre' => $nombre, 'email' => $email]);
    return $pdo->lastInsertId();
}

// READ (todos)
function obtenerUsuarios() {
    global $pdo;
    $stmt = $pdo->query("SELECT * FROM usuarios ORDER BY id DESC");
    return $stmt->fetchAll();
}

// READ (uno)
function obtenerUsuario($id) {
    global $pdo;
    $stmt = $pdo->prepare("SELECT * FROM usuarios WHERE id = :id");
    $stmt->execute(['id' => $id]);
    return $stmt->fetch();
}

// UPDATE
function actualizarUsuario($id, $nombre, $email) {
    global $pdo;
    $sql = "UPDATE usuarios SET nombre = :nombre, email = :email WHERE id = :id";
    $stmt = $pdo->prepare($sql);
    return $stmt->execute(['id' => $id, 'nombre' => $nombre, 'email' => $email]);
}

// DELETE
function eliminarUsuario($id) {
    global $pdo;
    $stmt = $pdo->prepare("DELETE FROM usuarios WHERE id = :id");
    return $stmt->execute(['id' => $id]);
}
?>
```

### SQL injection: por qué usar prepared statements

```php
// ❌ PELIGROSO: concatenar strings
$id = $_GET['id'];
$stmt = $pdo->query("SELECT * FROM usuarios WHERE id = $id");
// Un atacante puede pasar: ?id=1; DROP TABLE usuarios--

// ✅ SEGURO: prepared statements
$stmt = $pdo->prepare("SELECT * FROM usuarios WHERE id = :id");
$stmt->execute(['id' => $_GET['id']]);
```

## Node.js básico

Node.js permite ejecutar JavaScript en el servidor. Usaremos el módulo `http` y el framework Express.

### Servidor HTTP básico

```javascript
// server.js
const http = require('http');

const server = http.createServer((req, res) => {
    res.writeHead(200, { 'Content-Type': 'text/html' });
    res.end('<h1>Hola desde Node.js!</h1>');
});

server.listen(3000, () => {
    console.log('Servidor en http://localhost:3000');
});
```

### Express.js

```javascript
// Instalar: npm install express
const express = require('express');
const app = express();
const port = 3000;

// Middleware para parsear JSON
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// Rutas
app.get('/', (req, res) => {
    res.send('<h1>Inicio</h1>');
});

app.get('/api/usuarios', (req, res) => {
    res.json({ usuarios: [] });
});

app.post('/api/usuarios', (req, res) => {
    const { nombre, email } = req.body;
    res.status(201).json({ message: 'Usuario creado', data: req.body });
});

app.listen(port, () => {
    console.log(`Servidor en http://localhost:${port}`);
});
```

### Conexión a MySQL con mysql2

```javascript
// Instalar: npm install mysql2
const mysql = require('mysql2/promise');

const pool = mysql.createPool({
    host: 'localhost',
    user: 'root',
    password: '',
    database: 'mi_proyecto',
    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
});

module.exports = pool;
```

### CRUD completo con Express + MySQL

```javascript
const express = require('express');
const pool = require('./db');
const app = express();
app.use(express.json());

// CREATE
app.post('/api/usuarios', async (req, res) => {
    try {
        const { nombre, email } = req.body;
        const [result] = await pool.execute(
            'INSERT INTO usuarios (nombre, email) VALUES (?, ?)',
            [nombre, email]
        );
        res.status(201).json({ id: result.insertId, nombre, email });
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

// READ (todos)
app.get('/api/usuarios', async (req, res) => {
    try {
        const [rows] = await pool.query('SELECT * FROM usuarios');
        res.json(rows);
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

// READ (uno)
app.get('/api/usuarios/:id', async (req, res) => {
    try {
        const [rows] = await pool.execute(
            'SELECT * FROM usuarios WHERE id = ?', [req.params.id]
        );
        if (rows.length === 0) {
            return res.status(404).json({ error: 'No encontrado' });
        }
        res.json(rows[0]);
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

// UPDATE
app.put('/api/usuarios/:id', async (req, res) => {
    try {
        const { nombre, email } = req.body;
        await pool.execute(
            'UPDATE usuarios SET nombre = ?, email = ? WHERE id = ?',
            [nombre, email, req.params.id]
        );
        res.json({ message: 'Actualizado' });
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

// DELETE
app.delete('/api/usuarios/:id', async (req, res) => {
    try {
        await pool.execute('DELETE FROM usuarios WHERE id = ?', [req.params.id]);
        res.json({ message: 'Eliminado' });
    } catch (error) {
        res.status(500).json({ error: error.message });
    }
});

app.listen(3000);
```

## Buenas prácticas

1. **Validar y sanitizar**: nunca confíes en datos del usuario
2. **Prepared statements**: siempre para evitar SQL injection
3. **Manejo de errores**: try-catch en cada operación; responde con códigos HTTP adecuados
4. **Variables de entorno**: no hardcodees credenciales (usa `.env`)
5. **Separar capas**: rutas, controladores, modelos en archivos distintos

## Ejercicio práctico

Implementa un backend completo (PHP o Node.js) que gestione una lista de tareas (to-do list):
- Crear tarea (texto, fecha límite)
- Listar todas las tareas
- Marcar tarea como completada
- Eliminar tarea

<details>
<summary>Solución Node.js</summary>

```javascript
const express = require('express');
const pool = require('./db');
const app = express();
app.use(express.json());

// Crear tabla
// CREATE TABLE tareas (
//   id INT AUTO_INCREMENT PRIMARY KEY,
//   texto VARCHAR(255) NOT NULL,
//   fecha_limite DATE,
//   completada BOOLEAN DEFAULT FALSE,
//   creada_en TIMESTAMP DEFAULT CURRENT_TIMESTAMP
// );

app.post('/api/tareas', async (req, res) => {
    const { texto, fecha_limite } = req.body;
    await pool.execute(
        'INSERT INTO tareas (texto, fecha_limite) VALUES (?, ?)',
        [texto, fecha_limite]
    );
    res.status(201).json({ message: 'Tarea creada' });
});

app.get('/api/tareas', async (req, res) => {
    const [rows] = await pool.query('SELECT * FROM tareas ORDER BY creada_en DESC');
    res.json(rows);
});

app.put('/api/tareas/:id/completar', async (req, res) => {
    await pool.execute(
        'UPDATE tareas SET completada = NOT completada WHERE id = ?',
        [req.params.id]
    );
    res.json({ message: 'Estado actualizado' });
});

app.delete('/api/tareas/:id', async (req, res) => {
    await pool.execute('DELETE FROM tareas WHERE id = ?', [req.params.id]);
    res.json({ message: 'Tarea eliminada' });
});

app.listen(3000);
```

</details>

## Resumen

- **PHP**: lenguaje clásico del lado servidor con PDO para acceso seguro a BD
- **Node.js**: JavaScript del lado servidor con Express como framework minimalista
- **CRUD**: operaciones básicas sobre datos (Create, Read, Update, Delete)
- **Prepared statements**: protección esencial contra SQL injection
- **Middleware**: funciones que procesan peticiones antes de llegar a las rutas
